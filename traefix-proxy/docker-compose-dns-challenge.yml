services:

  traefik:
    image: "traefik:latest"
    container_name: "traefik"
    command:
      - "--log.level=INFO"
      - "--api=false"
      # - "--entrypoints.dashboard.address=:9090"
      # - "--api.dashboard=true"
      # - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.file.directory=/dynamic-conf"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.web.forwardedHeaders.trustedIPs=0.0.0.0/0"
      - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
      - "--entrypoints.web.http.redirections.entryPoint.scheme=https"
      - "--entrypoints.web.http.redirections.entrypoint.permanent=true"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.websecure.forwardedHeaders.trustedIPs=0.0.0.0/0"
      - "--entrypoints.websecure.http.tls.options=default@file"
      - "--certificatesresolvers.myresolver.acme.dnschallenge=true"
      - "--certificatesresolvers.myresolver.acme.dnschallenge.provider=dynu"
      - "--certificatesresolvers.myresolver.acme.email=user@example.org"
      - "--certificatesresolvers.myresolver.acme.storage=/letsencrypt/acme.json"
      # - "--certificatesresolvers.myresolver.acme.dnschallenge.resolvers=192.168.10.1,192.168.11.1"
      # - "--certificatesresolvers.myresolver.acme.dnschallenge.propagation.delayBeforeChecks=30s"
      # - "--certificatesresolvers.myresolver.acme.dnschallenge.propagation.disableanschecks=true"
      - "--providers.docker.network=traefik-proxy"
    # env_file: .env
    environment:
      LEGO_EXPERIMENTAL_CNAME_SUPPORT: true
      DYNU_API_KEY: ''
      DYNU_HTTP_TIMEOUT: 30
      DYNU_PROPAGATION_TIMEOUT: 600
      DYNU_POLLING_INTERVAL: 60
      DYNU_TTL: 180
    network_mode: host
    # ports:
    #   - 0.0.0.0:80:80
    #   - 0.0.0.0:443:443
    # networks:
    #   - proxy
    volumes:
      - "./letsencrypt:/letsencrypt:rw"
      # - "./certs:/etc/ssl/certs:ro"
      - "./dynamic-conf:/dynamic-conf:ro"
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    restart: unless-stopped

  whoami:
    image: "traefik/whoami:latest"
    container_name: "whoami"
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.whoami.rule=Host(`whoami.example.org`)"
      - "traefik.http.routers.whoami.entrypoints=websecure"
      - "traefik.http.routers.whoami.tls=true"
      - "traefik.http.routers.whoami.tls.certresolver=myresolver"
      - "traefik.http.services.whoami.loadbalancer.serversTransport=upload@file"
    restart: unless-stopped

networks:
  proxy:
    external: true
    name: traefik-proxy

